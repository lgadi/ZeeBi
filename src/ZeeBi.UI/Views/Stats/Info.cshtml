@model ZeeBi.UI.ViewModels.Stats.StatsViewModel

@{
    ViewBag.Title = "Info";
}

<h2>Info for: @Model.Url.Id, long URL: @Model.Url.LongUrl</h2>
Total page views: @Model.PageViewCount

<div class="table">
	<table>
		<thead>
			<td>UserAgent</td>
			<td>Count</td>
		</thead>
		<tbody>
		@foreach (var kvp in Model.PageViewsByUserAgent)
		 {
			<tr>
			<td>@kvp.Key</td>
			<td>@kvp.Value</td>
			</tr>
		 }
		</tbody>
	</table>
</div>

